<div class="block">
  <div class="secondary-navigation">
    <ul>
      <li class="first active"><%= link_to 'List', poll_anketas_path(@poll) %></li>
      <li><%= link_to 'New', new_poll_anketa_path(@poll) %></li>
    </ul>
    <div class="clear"></div>
  </div>          
  <div class="content">          
    <h2 class="title">All Anketas</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
					
					<th>Title</th>
					
          <th>Created at</th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @anketas.each do |anketa| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= anketa.id %>
          </td>
					
					<td>
						<%= link_to anketa.title, poll_anketa_path(@poll,anketa) %>
					</td>
					
					<td>
            <%= anketa.created_at %>
          </td>
          <td class="last">
            <%= link_to 'show', poll_anketa_path(@poll,anketa) %> |
            <%= link_to 'edit', edit_poll_anketa_path(@poll,anketa) %> |
            <%= link_to 'destroy', poll_anketa_path(@poll,anketa), :method => :delete, :confirm => 'Are you sure?' %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar">
        <div class="actions">
        </div>
        
        <%= will_paginate @anketas  %>
        
        <div class="clear"></div>
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>